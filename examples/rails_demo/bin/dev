#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR"

if command -v foreman >/dev/null 2>&1; then
  exec foreman start -f Procfile "$@"
fi

echo "foreman is not installed; starting rails server directly"
exec ./bin/rails server "$@"
